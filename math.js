var oninPrice = 100;
var eggPrice = 89;

var totalPrice = oninPrice + eggPrice;

var smartPhonePrice = 10000;
var faturePhonePrice = 1200;
var totalPhonePrice = (smartPhonePrice - faturePhonePrice);

// console.log(totalPrice)
// console.log(totalPhonePrice);

var mousePrice = 375;
var totalQuenty = 17;
var multiplication = (mousePrice * totalQuenty);

// console.log(multiplication);

var pcPrice = 200000;
var phnPrice = 1000;
var divisionPrice = (pcPrice + phnPrice);
console.log = (divisionPrice);

